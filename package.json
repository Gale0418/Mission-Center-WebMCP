{
  "name": "mission-center-webmcp",
  "version": "1.0.0-challenge",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "verify": "node scripts/verify.mjs",
    "build:site": "node scripts/build-site.mjs"
  },
  "engines": { "node": ">=20" }
}
